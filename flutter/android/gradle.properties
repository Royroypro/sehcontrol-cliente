org.gradle.jvmargs=-Xmx2g
android.useAndroidX=true
android.enableJetifier=true
org.gradle.daemon=false
# Flutter defaults to Android Studio's bundled JBR (JDK 21) regardless of the
# shell's JAVA_HOME, but this project's Gradle 7.6.4 + AGP 7.3.1 don't
# support JDK 21 ("Unsupported class file major version 65"). Pin Gradle to
# JDK 17 specifically, which this project's toolchain does support.
org.gradle.java.home=C:/Program Files/Java/jdk-17
